// Test: Chained method calls
// Expected output:
// 100
// 200

class Acc {
  public total: i32;

  public constructor(start: i32) {
    this.total = start;
  }

  public add(n: i32): i32 {
    this.total = this.total + n;
    return this.total;
  }

  public val(): i32 {
    return this.total;
  }
}

let acc = new Acc(0);
acc.add(50);
acc.add(50);
print acc.val();

acc.add(100);
print acc.val();
