interface Speaker {
  function speak(): string;
}

class Robot implements Speaker {
  function speak(): string {
    return "beep";
  }
}

function main(): void {
  let s: Speaker = new Robot();
  println(s.speak()); // expect: beep
}
