# Plan: Next 10 commits (C4u–C5d)

| Field | Value |
| ----- | ----- |
| **Date** | 2026-07-16 |
| **Status** | **Done** — C4u–C5d |
| **Baseline** | `C4t` done; RFCs 000–013 Accepted; branch `main` ahead of origin |
| **Sources** | `agents/debts.md` Open, `docs/roadmap.md` P3, nested mono bugs |
| **Rule** | One vertical slice per commit; tree green; update debts/roadmap/README/corpus |

## Overview

Ship ten P3 slices after **C4t**. Prefer: fix mono edge cases → small String API → Array/enum polish → Iterable duck → GC skeleton → ownership hygiene. Stay on C backend.

## Task list

1. **C4u** — Mono return-type audit: skip open (type-param) monomorphs; resolve method/fun return mono keys; nested generic field types
2. **C4v** — `String.isEmpty` (len == 0)
3. **C4w** — `String` char-at / byte-at (`charAt(i)` → Int byte or throw)
4. **C4x** — Array of enum elements **or** clear diagnostic reject (prefer reject + message if layout heavy)
5. **C4y** — Iterable duck: `for (x in c)` when `c` has `len` + `get(i)` (classes/structs)
6. **C4z** — GC STW mark-sweep skeleton (roots registry + no-op mark pass; still free-all on shutdown)
7. **C5a** — `std/collections` stub package note **or** Map RFC-only — prefer minimal `std` doc + debt update if no Map impl
8. **C5b** — Array move hygiene: reject/double-free path for `val b = a` shallow copy of owned Array (sema or free-once)
9. **C5c** — Multi-diagnostic collect (first N errors) **or** improve one high-traffic diagnostic
10. **C5d** — Close batch: debts/roadmap/README refresh; mark plan Done

## Sequential spine

C4u → C4v → C4w → C4x → C4y → C4z → C5a → C5b → C5c → C5d

## Out of batch

- Full Iterable protocol / interface design
- LLVM backend
- Registry / semver / git deps
- Channels / tasks / async
- Full mark-sweep collector (C4z is skeleton only)

## C4u detail (first slice)

**Bugs (reproduced):**

1. Nested `Wrapper<T>(Box<T>)` records open mono `Box_T` / `Wrapper_T` → C emits `aura_cls_T` and fails `cc`.
2. Method chain `m.makeBox().get()` resolves receiver as bare `Box` → wrong C symbol `aura_method_…_Box_get` instead of `Box_String_get`.

**Fix:**

- Sema `note_mono_ty` / mono_funs: skip args that still contain `TypeParam`; expand nested field monomorphs for concrete apps.
- Codegen `resolve_type_name`: substitute method/fun return types with mono type args (mirror `infer_type_name`).
- Method arg coercion uses class type-param substitution.
- Corpus: `generic/nested.aura` (+ optional method-return chain).
