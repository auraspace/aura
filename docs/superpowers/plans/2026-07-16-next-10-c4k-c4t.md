# Plan: Next 10 commits (C4k–C4t)

| Field | Value |
| ----- | ----- |
| **Date** | 2026-07-16 |
| **Baseline** | `C4j` done; branch `main` ahead of origin |
| **Sources** | `agents/debts.md` Open, broken `bounds.aura`, RFC-001 soft surface |
| **Rule** | One vertical slice per commit; tree green; update debts/roadmap/README/corpus |

## Overview

Ship ten P3 slices after **C4j**. Prefer: fix known regressions → syntax sugar → Array/String API → ownership hygiene → null-safe call. Stay on C backend; Iterable protocol still partial (defer full protocol; optional duck slice).

## Task list

1. **C4k** — Fix type-param / bound method `this` for heap classes (`bounds.aura`)
2. **C4l** — `else if` chaining in parser
3. **C4m** — Null coalesce `?:`
4. **C4n** — `Array.isEmpty`
5. **C4o** — `Array.reserve(n)`
6. **C4p** — `String.len` (UTF-8 byte length)
7. **C4q** — `Array` of struct by-value elements
8. **C4r** — Free Array buffer on reassignment of owner local
9. **C4s** — Safe call `?.` on nullable receivers
10. **C4t** — Nested `if` expression value OR multi-diag — pick: **if-expression** returning value

## Sequential spine

C4k → C4l → C4m → C4n → C4o → C4p → C4q → C4r → C4s → C4t

## Out of batch

- Full Iterable protocol, LLVM, registry/semver, channels/tasks
