# Plan: Next 10 commits (C5e–C5n)

| Field | Value |
| ----- | ----- |
| **Date** | 2026-07-16 |
| **Status** | **In progress** — start at C5e |
| **Baseline** | `C5d` done; plan C4u–C5d complete |
| **Sources** | `agents/debts.md` Open (Array move, GC sweep, collections) |
| **Rule** | One vertical slice per commit; tree green; update debts/roadmap/README/corpus |

## Overview

Ship ten P3 slices after **C5d**. Prefer: Array ownership → GC collect usable → small String/DX → batch close. Stay on C backend; no registry/LLVM.

## Task list

1. **C5e** — Array move on assign (`b = a` when `a` is owner; free left if owner; zero source)
2. **C5f** — GC `aura_gc_collect`: if roots registered → mark + **sweep** unmarked; if no roots → mark-all (safe no-op reclaim)
3. **C5g** — Codegen: `aura_gc_add_root` / `remove_root` for heap-class locals at scope enter/leave
4. **C5h** — `String.startsWith(prefix: String): Bool`
5. **C5i** — `String.contains(sub: String): Bool`
6. **C5j** — `String.endsWith(suffix: String): Bool`
7. **C5k** — Multi-error in function body (collect up to N stmt errors; report first via API, rest in message notes) **or** improve assign-type diagnostic
8. **C5l** — Array assign from non-owner Ident: document no free; optional deep-copy reject
9. **C5m** — Corpus + smoke for GC roots (class locals survive collect if rooted)
10. **C5n** — Close batch: debts/roadmap/README/plan Done

## Sequential spine

C5e → C5f → C5g → C5h → C5i → C5j → C5k → C5l → C5m → C5n

## Out of batch

- Full Iterable interface, Map/Set implementation, registry, LLVM, channels/tasks
