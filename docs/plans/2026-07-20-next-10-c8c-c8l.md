# Next 10: C8c–C8l

| Field      | Value                                                         |
| ---------- | ------------------------------------------------------------- |
| **Opened** | 2026-07-20                                                    |
| **After**  | C8a generic Map + C8b path lock polish                        |
| **Goal**   | Generic iface mono, Iterable, nested Array, collections, lock |

## Batch

| ID      | Slice                                                                              | Status |
| ------- | ---------------------------------------------------------------------------------- | ------ |
| **C8c** | Generic interface implements mono (`: Iface<T>`; `Ty::InterfaceApp`; codegen mono) | Done   |
| **C8d** | `std.collections` `Iterable<E>` + for-in on mono iface                             | Done   |
| **C8e** | Nested `Array<Array<T>>` mono (emit order + elem mono note)                        | Done   |
| **C8f** | Free nested Array element buffers on drop/clear/set                                | Done   |
| **C8g** | Generic `Set<T>` (linear)                                                          | Done   |
| **C8h** | for-in over `Map.keys` / `Set` (duck `get`)                                        | Done   |
| **C8i** | `HashMap` String→Int open addressing + `hash_string`                               | Done   |
| **C8j** | Non-destructive Array field bind (view); return still moves                        | Done   |
| **C8k** | `aura.lock` registry schema v0 (parse version/source/checksum)                     | Done   |
| **C8l** | Close C8c–C8l batch (roadmap / debts / this plan)                                  | Done   |

## Out of scope this batch

- Registry HTTP client / semver resolver (schema only in C8k)
- True `ref`/borrow types (C8j is view without move, not a type)
- `Array` of interface elements (still reject per C7h)
- Generic `HashMap<K,V>` / resize
- LLVM backend, tasks/channels
