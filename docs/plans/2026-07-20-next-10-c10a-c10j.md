# Next 10: C10a–C10j

| Field      | Value                                                                      |
| ---------- | -------------------------------------------------------------------------- |
| **Opened** | 2026-07-20                                                                 |
| **After**  | C9j close (generic class implements, Array.clone, String+, type/const, is) |
| **Goal**   | DX polish + start first-class functions / lambdas; keep slices shippable   |

## Batch

| ID       | Slice                                                               | Status   |
| -------- | ------------------------------------------------------------------- | -------- |
| **C10a** | Plan + roadmap C10a–C10j                                            | Done     |
| **C10b** | Diagnostics polish: context line + notes on pretty errors           | Done     |
| **C10c** | Parse lambdas `(x: T) => expr` / `() => expr`                       | Done     |
| **C10d** | Sema: `Ty::Fun`, lambda body check, call through fun value          | Done     |
| **C10e** | Codegen: non-capturing lambdas → static C fn + function pointer     | Done     |
| **C10f** | Fun type syntax `(T) -> U` in type positions (params / annotations) | Done     |
| **C10g** | Lambdas with block body `(x: T) => { … }`                           | Done     |
| **C10h** | Captures / closures (`val` Int/Bool/String MVP)                     | Done     |
| **C10i** | Higher-order std helpers / more corpus                              | Deferred |
| **C10j** | Close C10 batch (roadmap / debts / this plan)                       | Pending  |

## Shipped notes

- **C10b:** `format_error_with` — one context line above + auto `= note:` for type-mismatch wording; CLI uses `context_before: true`.
- **C10c–f:** `(x: Int) => x + 1`, `() => 0`, `Ty::Fun`, call through fun value, `(Int) -> Int` annotations / params.
- **C10g:** `LambdaBody::{Expr,Block}`; parse `{…}` after `=>`; sema block + return infer; codegen `emit_block` in static lambda fn.
- **C10h:** fat-pointer Fun `{ void *env; fn }`; capture outer immutable `val` Int/Bool/String (heap env, copy-out); reject `var` / other types.
- Corpus: `lambda_basic`, `lambda_zero`, `lambda_fun_type`, `lambda_param`, `lambda_block`, `lambda_capture`.

## Out of scope this batch

- `var` capture, class/Array/Fun capture, env free/GC
- `async` / tasks, macros, LLVM
- Generic `HashMap<K,V>`, registry HTTP client
- True `ref`/borrow types
- Array of interface elements
