[package]
name = "aura-rt"
version = "0.1.0"
edition = "2021"
build = "build.rs"

[lib]
name = "aura_rt"
crate-type = ["staticlib", "rlib"]

[dependencies]
# Minimal runtime, no external dependencies for now.

[build-dependencies]
cc = "1"
