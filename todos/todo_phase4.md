# Todo: Phase 4 - Incremental Features

**Focus**: Basic Control Flow (If/Else, While) and Comparisons.

- [x] Add Comparison Operators to Lexer and Token kinds (`==`, `!=`, `<`, `>`, `<=`, `>=`)
- [x] Add `if`, `else`, `while` keywords to Lexer
- [x] Update AST to support `If` statements, `While` loops, and `Block` statements
- [x] Implement Parsing for `if/else`, `while`, and comparison expressions
- [x] Implement Codegen for comparison operators and conditional branching (labels)
- [x] Implement Codegen for `If` and `While` statements
- [x] Verify with an Aura script that calculates something (e.g., a simple loop to sum numbers)
- [x] Implement Function definitions and calls (including recursion!)
- [x] Implement basic Objects (classes)
