package demo.stdcollections.list

import std.collections

fun main() {
  val xs = List.of(1, 2, 3)
  val ys: List<String> = xs.map<String>((x: Int) => x.toString())
  println(ys.get(0))
  println(ys.get(2))
  val zs = listOf("a", "b", "c")
  println(zs.len().toString())
}
