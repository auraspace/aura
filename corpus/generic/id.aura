// C2b corpus — generic free function
package demo.generic

fun id<T>(x: T): T {
  return x
}

fun main() {
  val s: String = id<String>("Hello, Aura")
  println(s)
  val n: Int = id<Int>(42)
  // no int println yet — just exercise mono
  val again: String = id<String>("ok")
  println(again)
}
