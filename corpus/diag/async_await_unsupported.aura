// C22p boundary fixture — await nested in control flow remains outside the
// bounded straight-line state-machine slice. Direct top-level return await is
// covered by the codegen regression suite.
package demo.tasks.diag

async fun answer(task: Task<Int>): Int {
  if (true) {
    return await task
  }
  return 0
}
