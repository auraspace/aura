// C22p — bounded suspension/resumption plus cooperative cancellation.
package demo.tasks.lifecycle

async fun answer(): Int {
  return 7
}

fun main() {
  val resumed = spawn {
    val value: Int = await answer()
    println("resumed")
    return
  }
  join(resumed)

  val cancelled = spawn {}
  cancel(cancelled)
}
